#include "main.h"
/**
 * _islower - returns 1 on lowercase and 0 on non
 *
 * c - integer to check
 *
 * Return: 0 or 1
 */
int _islower(int c)
{
if (c >= 97 && c <= 122)
return (1);
else
return (0); }
