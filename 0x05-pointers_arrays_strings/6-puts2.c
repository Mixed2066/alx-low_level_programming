#include "main.h"
/**
 * puts2 - Prints every second character of a string
 * @str: The input string
 *
 */
void puts2(char *str)
{
int i;
for (i = 0; i <= str[i]; i++)
{
if (i % 2 == 0)
_putchar(str[i]);
else
continue;
}
_putchar('\n');
}
